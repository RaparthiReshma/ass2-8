#include <iostream>

int main() {
    int n;
    int r,ecount=0,ocount=0;
    scanf("%d",&n);
    while(n>0)
    {
        r=n%10;
        if(r%2==0)
        ecount=ecount+1;
    else
        ocount=ocount+1;
        n=n/10;
    }
    printf("even count %d\n",ecount);
    printf("\n odd count %d",ocount);
    if(ecount=ocount)
        printf("fancy number");
    else
        printf("not fancy number");
    return 0;
}
